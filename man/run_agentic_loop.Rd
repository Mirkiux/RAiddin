% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools_loop.R
\name{run_agentic_loop}
\alias{run_agentic_loop}
\title{Run the RAiddin agentic loop}
\usage{
run_agentic_loop(
  messages,
  system = NULL,
  tools = raiddin_tools(),
  model = getOption("raiddin.model", claude_default_model),
  max_tokens = getOption("raiddin.max_tokens", claude_default_max_tokens),
  envir = .GlobalEnv,
  max_turns = 10L,
  on_approval_required = NULL
)
}
\arguments{
\item{messages}{A list of message objects built with \code{\link[=user_message]{user_message()}} and
\code{\link[=assistant_message]{assistant_message()}}.}

\item{system}{Optional system prompt string.}

\item{tools}{List of tool definitions. Defaults to \code{\link[=raiddin_tools]{raiddin_tools()}}.}

\item{model}{Claude model ID. Defaults to the \code{raiddin.model} option.}

\item{max_tokens}{Maximum tokens per response. Defaults to the
\code{raiddin.max_tokens} option.}

\item{envir}{Environment for code execution tools (default \code{.GlobalEnv}).}

\item{max_turns}{Maximum number of tool-use turns before stopping
(default \code{10L}). Prevents infinite loops if the model keeps calling tools.}

\item{on_approval_required}{Optional \verb{function(tool_call)} returning
\code{logical(1)}. Invoked when \code{\link[=check_code_approval]{check_code_approval()}} requires a prompt;
return \code{TRUE} to allow execution, \code{FALSE} to deny. When \code{NULL} (default),
unapproved code is denied automatically.}
}
\value{
A named list with:
\describe{
\item{\code{response}}{The final Claude API response list.}
\item{\code{messages}}{Full conversation history including all tool turns.}
\item{\code{turns}}{Number of tool-use turns completed.}
\item{\code{hit_limit}}{\code{logical(1)} — \code{TRUE} if the loop stopped because
\code{max_turns} was reached.}
}
}
\description{
Calls the Claude API with \code{messages} and \code{tools}, then handles any
\code{tool_use} turns automatically: each tool call is dispatched via
\code{dispatch_tool()}, the results are appended to the conversation, and
Claude is called again. The loop stops when Claude returns \code{end_turn},
when \code{stop_reason} is not \code{"tool_use"}, or when \code{max_turns} is reached.
}
\details{
Tool calls are never streamed; \code{\link[=chat_completion]{chat_completion()}} is used throughout so
that \code{tool_use} content blocks are preserved for dispatch.
}
