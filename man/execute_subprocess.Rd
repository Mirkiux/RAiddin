% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execution_subprocess.R
\name{execute_subprocess}
\alias{execute_subprocess}
\title{Execute R code in an isolated callr subprocess}
\usage{
execute_subprocess(
  code,
  envir = .GlobalEnv,
  threshold_mb = 50,
  always_push = character(0),
  timeout_s = Inf
)
}
\arguments{
\item{code}{A character(1) string of R code to evaluate.}

\item{envir}{The environment to replicate objects from (default \code{.GlobalEnv}).}

\item{threshold_mb}{Numeric; objects above this size (in MB) are not
auto-pushed (default \code{50}).}

\item{always_push}{Character vector of object names to always push,
regardless of \code{threshold_mb}.}

\item{timeout_s}{Numeric; seconds to allow the subprocess to run before
forcibly terminating it (default \code{Inf}, no limit). Use a finite value to
prevent runaway code from hanging the session indefinitely.}
}
\value{
A named list with elements \code{stdout}, \code{stderr}, \code{plots}, \code{error},
\code{warnings}, \code{messages}, and \code{large} (names of skipped large objects).
}
\description{
Runs \code{code} in a persistent background R session, replicating the objects
from \code{envir} that the code references. Objects larger than \code{threshold_mb}
are not pushed; their names are returned in \verb{$large} so callers can decide
what to do (e.g., prompt the user).
}
