% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_client.R
\name{chat_completion_stream}
\alias{chat_completion_stream}
\title{Send a streaming messages request to the Claude API}
\usage{
chat_completion_stream(
  messages,
  on_text = NULL,
  model = getOption("raiddin.model", claude_default_model),
  max_tokens = getOption("raiddin.max_tokens", claude_default_max_tokens),
  tools = NULL,
  system = NULL
)
}
\arguments{
\item{messages}{A list of message objects.}

\item{on_text}{Optional callback \verb{function(delta)} invoked with each text
delta string as it arrives.}

\item{model}{Claude model ID.}

\item{max_tokens}{Maximum tokens to generate.}

\item{tools}{Optional list of tool definition objects.}

\item{system}{Optional system prompt string.}
}
\value{
A named list with \code{role}, \code{content}, and \code{stop_reason}, mirroring
the structure of a \code{\link[=chat_completion]{chat_completion()}} response.
}
\description{
Like \code{\link[=chat_completion]{chat_completion()}} but streams the response token by token, calling
\code{on_text} incrementally. Returns the fully assembled response once complete.
Only text content is streamed; if \code{tools} is supplied and the model
responds with \code{tool_use}, call \code{\link[=chat_completion]{chat_completion()}} instead so tool_use
blocks are preserved for \code{\link[=extract_tool_calls]{extract_tool_calls()}} and
\code{\link[=tool_result_message]{tool_result_message()}}.
}
